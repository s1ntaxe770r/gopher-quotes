apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gopher-quotes-ingress
  annotations:
    nginx.ingress.kubernetes.io/app-root: /
spec:
  ingressClassName: nginx
  rules:
  - host: "3ac0ded5-8263-4bac-b68b-9df033c7ee44.k8s.civo.com"
    http:
      paths:
      - path: /quotes
        pathType: Exact
        backend:
          service:
            name: gopher-quotes-frontend
            port:
              number: 80

